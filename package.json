{
  "name": "batch-cep-plugin",
  "version": "0.1.0",
  "description": "batch-cep Claude skill — orchestrate Batch.com via natural language",
  "type": "module",
  "private": true,
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "lint": "biome check .",
    "lint:fix": "biome check --write ."
  },
  "devDependencies": {
    "@biomejs/biome": "^1.9.4",
    "vitest": "^2.1.9"
  },
  "engines": { "node": ">=18" }
}
